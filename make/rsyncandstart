#!/bin/bash

rsync -ravz --delete . deploy:$DEPLOYDIR/
ssh deploy 'bash -c "export NVM_DIR=\"\$HOME/.nvm\" && \. \"\$NVM_DIR/nvm.sh\" && '$RELOADCOMMAND'"; sudo nginx -t && sudo service nginx reload' && ls -la
