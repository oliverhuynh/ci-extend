#!/bin/bash

echo "VERY STUPID rsync -ravz --delete . deploy:$DEPLOYDIR/"
# rsync -ravz --delete . deploy:$DEPLOYDIR/
echo 'bash -c "export NVM_DIR=\"\$HOME/.nvm\" && \. \"\$NVM_DIR/nvm.sh\" && '$RELOADCOMMAND'"; sudo nginx -t && sudo service nginx reload' && ls -la
